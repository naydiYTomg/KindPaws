<MudDialog Style="min-width: 90%; min-height: 90%; background-color: var(--mud-palette-primary)">
    <TitleContent>
        <MudText Typo="Typo.h5" Class="kp-all-text">Отзывы</MudText>
    </TitleContent>
    <DialogContent>
        <MudText Typo="Typo.h1" Class="kp-all-text">ЗАПОЛНИТЕЛЬ</MudText>
    </DialogContent>
    @* <DialogActions> *@
    @*     <MudButton Variant="Variant.Filled" Color="Color.Tertiary" OnClick="Close">X</MudButton> *@
    @* </DialogActions> *@
</MudDialog>
@code {
    [CascadingParameter]
    private IMudDialogInstance Dialog { get; set; }

    private void Submit() => Dialog.Close(DialogResult.Ok(true));
}